my_app_homepage:
    pattern:  /
    defaults:
        _controller: MyAppBundle:Default:index

login:
    pattern: /login
    defaults:
        _controller: MyAppBundle:Default:login

logout:
    pattern: /logout
    defaults:
        _controller: MyAppBundle:Default:logout

login_check:
    pattern: /login_check
    defaults:
        _controller: MyAppBundle:Default:loginCheck

foo:
    pattern: /foo/
    defaults:
        _controller: MyAppBundle:Default:foo